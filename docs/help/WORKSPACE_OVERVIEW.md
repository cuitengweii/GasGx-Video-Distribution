# 工作台总览

## 用途

工作台总览用于快速确认当前 GasGx Video Distribution 的运营状态，包括账号规模、任务状态、素材余量、发布结果和系统健康提示。

## 主要查看项

- 账号矩阵数量与启用状态。
- 可用素材、已生成视频和待处理任务。
- 发布成功、失败、重试中的任务概况。
- 数据库或本地数据库健康状态。

## 操作建议

- 每次批量发布前先查看总览，确认素材和账号状态没有明显异常。
- 如果总览显示任务失败或登录失效，先进入通知中心或任务中心定位原因。
- 如果数据为空，先检查系统设置里的数据库初始化和数据库健康状态。
